-- This will select all the columns from a table,
-- and only show the first 10 rows of data.
SELECT * 
FROM name_of_table 
LIMIT 10;

-------------------------------------------------------------------------------

-- To view data from specific columns
SELECT collumn_name, collumn_name
FROM name_of_table
LIMIT 10;
